{% extends 'base.html.twig' %}

{% block title %}MEMO{% endblock %}

{% block body %}
    <h1>MEMO</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ m_e_m_o.id }}</td>
            </tr>
            <tr>
                <th>Titel</th>
                <td>{{ m_e_m_o.Titel }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ m_e_m_o.Content }}</td>
            </tr>
            <tr>
                <th>Updated_at</th>
                <td>{{ m_e_m_o.UpdatedAt ? m_e_m_o.UpdatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('m_e_m_o_index') }}">back to list</a>

    <a href="{{ path('m_e_m_o_edit', {'id': m_e_m_o.id}) }}">edit</a>

    {{ include('m_e_m_o/_delete_form.html.twig') }}
{% endblock %}
