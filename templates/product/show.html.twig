{% extends 'base.html.twig' %}

{% block title %}Product{% endblock %}

{% block body %}
    <h1>Product</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product.id }}</td>
            </tr>
            <tr>
                <th>Naam</th>
                <td>{{ product.Naam }}</td>
            </tr>
            <tr>
                <th>Omschrijving</th>
                <td>{{ product.Omschrijving }}</td>
            </tr>
            <tr>
                <th>Prijs</th>
                <td>{{ product.Prijs }}</td>
            </tr>
            <tr>
                <th>Frontpage</th>
                <td>{{ product.Frontpage }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('product_index') }}">back to list</a>

    <a href="{{ path('product_edit', {'id': product.id}) }}">edit</a>

    {{ include('product/_delete_form.html.twig') }}
{% endblock %}
